# GOALS — mục tiêu đang theo

Đọc cùng `memory/MEMORY.md` ở đầu mỗi session. Mỗi ca mới, kiểm tra xem nó có
đẩy hoặc cản goal nào không. Goal chỉ được thêm khi người hỏi xác nhận.

## Đang theo

*(chưa có goal nào)*

Format mỗi goal:

```
### [tên goal]
- Đặt ngày: YYYY-MM-DD · Hạn/mốc: ...
- Vì sao (giá trị đằng sau, `2.1`):
- Trạng thái: đang chạy | kẹt | tạm dừng
- Ca liên quan: [link file trong memory/cases/]
- Cập nhật gần nhất: YYYY-MM-DD — một dòng
```

## Đã đóng

*(chưa có)*
